{
  "name": "@escalated-dev/plugin-nps",
  "version": "0.1.0",
  "description": "Net Promoter Score surveys with automated scheduling and analytics — TypeScript SDK version",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@escalated-dev/plugin-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
